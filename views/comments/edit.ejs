<% include ../partials/header %>

<h1 style="text-align:center">edit</h1>
<div class="container">
    <div class="row">
        <div style="width:30% ;margin:30px auto">
            <form action="/campground/<%=campgroundid%>/comments/<%=comment.id%>?_method=PUT" method="POST">
                <div class="form-group">
                    <textarea name="comment[text]" rows="10" cols="45" required><%=comment.text%></textarea>
                </div>
                <div class="form-group">
                    <input class="btn btn-primary btn-lg btn-block" type="submit">
                </div>
            </form>
            <a href="/campground/<%=campgroundid%>">do u wish to go back ?</a>
        </div>
    </div>
</div>

<% include ../partials/footer %>